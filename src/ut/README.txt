Naming convention:

foo_test.cpp is a suite of unit tests for the production foo.

fakefoo.cpp is a fake (or stub) foo.

test_foo_* is support data for the test of foo.

test_foo.cpp is support code (e.g., test_main, test_utils).

